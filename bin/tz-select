#!/bin/bash

set -euo pipefail

timezone=$(timedatectl list-timezones | fuzzel --dmenu --prompt "Timezone > ") || exit 0
[[ -z "$timezone" ]] && exit 0

sudo timedatectl set-timezone "$timezone"
restart-waybar
